# Global settings for Study Pal (stub values).

logging:
  level: INFO

scheduler:
  default_persona: "Kobe Bryant"
  timezone: "UTC"

rag:
  embedding_model: "text-embedding-3-large"
  vector_store: "chroma"

